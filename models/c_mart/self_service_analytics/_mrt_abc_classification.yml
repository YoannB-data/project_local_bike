version: 2

models:
  - name: mrt_abc_classification
    description: '{{ doc("mrt_abc_classification") }}'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null