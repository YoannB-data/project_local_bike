version: 2

models:
  - name: fct__abc_classification
    description: '{{ doc("fct__abc_classification") }}'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null