version: 2

models:
  - name: mrt__abc_classification
    description: '{{ doc("mrt__abc_classification") }}'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null