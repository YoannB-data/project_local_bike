version: 2

models:
  - name: int_sales__order_items
    description: |
      This model calcules the net revenue per item sold for each order.
    columns:
      - name: order_item_id
        description: "Primary key, unique identifier for each item per order."
        tests:
          - unique
          - not_null