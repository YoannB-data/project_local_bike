version: 2

models:
  - name: int_production__products
    description: |
      This model aggregates products data.
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null